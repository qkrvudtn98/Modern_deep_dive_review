/** Array.prototype.reverse 메서드 
 * reverse 메서드는 원본 배열을 반대로 뒤집는다 (원본 배열 변경) => 반환값 : 변경된 배열 
 */

const arr = [1,2,3];
const result = arr.reverse();

// reverse 메서드는 원본 배열을 직접 변경한다 
console.log(arr);
// 반환값은 변경된 배열이다 
console.log(result);
